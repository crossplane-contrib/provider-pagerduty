---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: groupingsettings.alert.pagerduty.crossplane.io
spec:
  group: alert.pagerduty.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - pagerduty
    kind: GroupingSetting
    listKind: GroupingSettingList
    plural: groupingsettings
    singular: groupingsetting
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GroupingSetting is the Schema for the GroupingSettings API. Creates
          and manages an alert grouping setting in PagerDuty.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GroupingSettingSpec defines the desired state of GroupingSetting
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  config:
                    description: The set of values used for configuration.
                    items:
                      properties:
                        aggregate:
                          description: One of any or all. This setting is only required
                            and applies when type is set to content_based or content_based_intelligent.
                            Group alerts based on one or all of fields value(s).
                          type: string
                        fields:
                          description: Alerts will be grouped together if the content
                            of these fields match. This setting is only required and
                            applies when type is set to content_based or content_based_intelligent.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        iagFields:
                          description: |-
                            An array of strings which represent the iag fields with which to intelligently group against.  This setting applies only when type is set to intelligent. Default: ["summary"].
                            An array of strings which represent the iag fields with which to intelligently group against
                          items:
                            type: string
                          type: array
                        timeWindow:
                          description: The maximum amount of time allowed between
                            Alerts. This setting applies only when type is set to
                            intelligent, content_based, content_based_intelligent.
                            Value must be between 300 and 3600 or exactly 86400 (86400
                            is supported only for content_based alert grouping). Any
                            Alerts arriving greater than time_window seconds apart
                            will not be grouped together. This is a rolling time window
                            and is counted from the most recently grouped alert. The
                            window is extended every time a new alert is added to
                            the group, up to 24 hours. To use the recommended time
                            window leave this value unset or set it to null.
                          type: number
                        timeout:
                          description: The duration in seconds within which to automatically
                            group incoming alerts. This setting is only required and
                            applies when type is set to time. To continue grouping
                            alerts until the incident is resolved leave this value
                            unset or set it to null.
                          type: number
                      type: object
                    type: array
                  description:
                    description: A human-friendly text to describe and identify this
                      alert grouping setting.
                    type: string
                  name:
                    description: The name for the alert grouping settings.
                    type: string
                  serviceRefs:
                    description: References to Service in service to populate services.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  serviceSelector:
                    description: Selector for a list of Service in service to populate
                      services.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  services:
                    description: '[Updating can cause a resource replacement] The
                      list IDs of services associated to this setting.'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  type:
                    description: The type of alert grouping; one of intelligent, time,
                      content_based or  content_based_intelligent.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  config:
                    description: The set of values used for configuration.
                    items:
                      properties:
                        aggregate:
                          description: One of any or all. This setting is only required
                            and applies when type is set to content_based or content_based_intelligent.
                            Group alerts based on one or all of fields value(s).
                          type: string
                        fields:
                          description: Alerts will be grouped together if the content
                            of these fields match. This setting is only required and
                            applies when type is set to content_based or content_based_intelligent.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        iagFields:
                          description: |-
                            An array of strings which represent the iag fields with which to intelligently group against.  This setting applies only when type is set to intelligent. Default: ["summary"].
                            An array of strings which represent the iag fields with which to intelligently group against
                          items:
                            type: string
                          type: array
                        timeWindow:
                          description: The maximum amount of time allowed between
                            Alerts. This setting applies only when type is set to
                            intelligent, content_based, content_based_intelligent.
                            Value must be between 300 and 3600 or exactly 86400 (86400
                            is supported only for content_based alert grouping). Any
                            Alerts arriving greater than time_window seconds apart
                            will not be grouped together. This is a rolling time window
                            and is counted from the most recently grouped alert. The
                            window is extended every time a new alert is added to
                            the group, up to 24 hours. To use the recommended time
                            window leave this value unset or set it to null.
                          type: number
                        timeout:
                          description: The duration in seconds within which to automatically
                            group incoming alerts. This setting is only required and
                            applies when type is set to time. To continue grouping
                            alerts until the incident is resolved leave this value
                            unset or set it to null.
                          type: number
                      type: object
                    type: array
                  description:
                    description: A human-friendly text to describe and identify this
                      alert grouping setting.
                    type: string
                  name:
                    description: The name for the alert grouping settings.
                    type: string
                  serviceRefs:
                    description: References to Service in service to populate services.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  serviceSelector:
                    description: Selector for a list of Service in service to populate
                      services.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  services:
                    description: '[Updating can cause a resource replacement] The
                      list IDs of services associated to this setting.'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  type:
                    description: The type of alert grouping; one of intelligent, time,
                      content_based or  content_based_intelligent.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: GroupingSettingStatus defines the observed state of GroupingSetting.
            properties:
              atProvider:
                properties:
                  config:
                    description: The set of values used for configuration.
                    items:
                      properties:
                        aggregate:
                          description: One of any or all. This setting is only required
                            and applies when type is set to content_based or content_based_intelligent.
                            Group alerts based on one or all of fields value(s).
                          type: string
                        fields:
                          description: Alerts will be grouped together if the content
                            of these fields match. This setting is only required and
                            applies when type is set to content_based or content_based_intelligent.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        iagFields:
                          description: |-
                            An array of strings which represent the iag fields with which to intelligently group against.  This setting applies only when type is set to intelligent. Default: ["summary"].
                            An array of strings which represent the iag fields with which to intelligently group against
                          items:
                            type: string
                          type: array
                        timeWindow:
                          description: The maximum amount of time allowed between
                            Alerts. This setting applies only when type is set to
                            intelligent, content_based, content_based_intelligent.
                            Value must be between 300 and 3600 or exactly 86400 (86400
                            is supported only for content_based alert grouping). Any
                            Alerts arriving greater than time_window seconds apart
                            will not be grouped together. This is a rolling time window
                            and is counted from the most recently grouped alert. The
                            window is extended every time a new alert is added to
                            the group, up to 24 hours. To use the recommended time
                            window leave this value unset or set it to null.
                          type: number
                        timeout:
                          description: The duration in seconds within which to automatically
                            group incoming alerts. This setting is only required and
                            applies when type is set to time. To continue grouping
                            alerts until the incident is resolved leave this value
                            unset or set it to null.
                          type: number
                      type: object
                    type: array
                  description:
                    description: A human-friendly text to describe and identify this
                      alert grouping setting.
                    type: string
                  id:
                    description: The ID of the alert grouping setting.
                    type: string
                  name:
                    description: The name for the alert grouping settings.
                    type: string
                  services:
                    description: '[Updating can cause a resource replacement] The
                      list IDs of services associated to this setting.'
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  type:
                    description: The type of alert grouping; one of intelligent, time,
                      content_based or  content_based_intelligent.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
