---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: servicenows.extensions.pagerduty.m.crossplane.io
spec:
  group: extensions.pagerduty.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - pagerduty
    kind: Servicenow
    listKind: ServicenowList
    plural: servicenows
    singular: servicenow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Servicenow is the Schema for the Servicenows API. Creates and
          manages a ServiceNow service extension in PagerDuty.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServicenowSpec defines the desired state of Servicenow
            properties:
              forProvider:
                properties:
                  endpointUrlSecretRef:
                    description: |-
                      A LocalSecretKeySelector is a reference to a secret key
                      in the same namespace with the referencing object.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  extensionObjects:
                    description: This is the objects for which the extension applies
                      (An array of service ids).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  extensionSchema:
                    description: This is the schema for this extension.
                    type: string
                  name:
                    description: The name of the service extension.
                    type: string
                  referer:
                    description: The ServiceNow referer.
                    type: string
                  snowPasswordSecretRef:
                    description: The ServiceNow password.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  snowUser:
                    description: The ServiceNow username.
                    type: string
                  summary:
                    description: A short-form, server-generated string that provides
                      succinct, important information about an object suitable for
                      primary labeling of an entity in a client. In many cases, this
                      will be identical to name, though it is not intended to be an
                      identifier.
                    type: string
                  syncOptions:
                    description: The ServiceNow sync option.
                    type: string
                  target:
                    description: Target Webhook URL.
                    type: string
                  taskType:
                    description: The ServiceNow task type, typically incident.
                    type: string
                  type:
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  endpointUrlSecretRef:
                    description: |-
                      A LocalSecretKeySelector is a reference to a secret key
                      in the same namespace with the referencing object.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  extensionObjects:
                    description: This is the objects for which the extension applies
                      (An array of service ids).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  extensionSchema:
                    description: This is the schema for this extension.
                    type: string
                  name:
                    description: The name of the service extension.
                    type: string
                  referer:
                    description: The ServiceNow referer.
                    type: string
                  snowPasswordSecretRef:
                    description: The ServiceNow password.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  snowUser:
                    description: The ServiceNow username.
                    type: string
                  summary:
                    description: A short-form, server-generated string that provides
                      succinct, important information about an object suitable for
                      primary labeling of an entity in a client. In many cases, this
                      will be identical to name, though it is not intended to be an
                      identifier.
                    type: string
                  syncOptions:
                    description: The ServiceNow sync option.
                    type: string
                  target:
                    description: Target Webhook URL.
                    type: string
                  taskType:
                    description: The ServiceNow task type, typically incident.
                    type: string
                  type:
                    type: string
                required:
                - snowPasswordSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.extensionObjects is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.extensionObjects)
                || (has(self.initProvider) && has(self.initProvider.extensionObjects))'
            - message: spec.forProvider.extensionSchema is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.extensionSchema)
                || (has(self.initProvider) && has(self.initProvider.extensionSchema))'
            - message: spec.forProvider.referer is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.referer)
                || (has(self.initProvider) && has(self.initProvider.referer))'
            - message: spec.forProvider.snowPasswordSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.snowPasswordSecretRef)'
            - message: spec.forProvider.snowUser is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.snowUser)
                || (has(self.initProvider) && has(self.initProvider.snowUser))'
            - message: spec.forProvider.syncOptions is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.syncOptions)
                || (has(self.initProvider) && has(self.initProvider.syncOptions))'
            - message: spec.forProvider.target is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.target)
                || (has(self.initProvider) && has(self.initProvider.target))'
            - message: spec.forProvider.taskType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.taskType)
                || (has(self.initProvider) && has(self.initProvider.taskType))'
          status:
            description: ServicenowStatus defines the observed state of Servicenow.
            properties:
              atProvider:
                properties:
                  extensionObjects:
                    description: This is the objects for which the extension applies
                      (An array of service ids).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  extensionSchema:
                    description: This is the schema for this extension.
                    type: string
                  htmlUrl:
                    description: URL at which the entity is uniquely displayed in
                      the Web app.
                    type: string
                  id:
                    description: The ID of the extension.
                    type: string
                  name:
                    description: The name of the service extension.
                    type: string
                  referer:
                    description: The ServiceNow referer.
                    type: string
                  snowUser:
                    description: The ServiceNow username.
                    type: string
                  summary:
                    description: A short-form, server-generated string that provides
                      succinct, important information about an object suitable for
                      primary labeling of an entity in a client. In many cases, this
                      will be identical to name, though it is not intended to be an
                      identifier.
                    type: string
                  syncOptions:
                    description: The ServiceNow sync option.
                    type: string
                  target:
                    description: Target Webhook URL.
                    type: string
                  taskType:
                    description: The ServiceNow task type, typically incident.
                    type: string
                  type:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
